interface Props {
  children: React.ReactNode;
}

const AddSchoolRequestLayout = ({
  children,
}: Props) => {
  return (
    <div>
      {children}
    </div>
  );
};

export default AddSchoolRequestLayout;
