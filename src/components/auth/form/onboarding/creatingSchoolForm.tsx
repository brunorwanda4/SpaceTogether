import { Form } from "@/components/ui/form"
import { useForm } from "react-hook-form"

const CreatingSchoolForm = () => {
    // const form = useForm<>()
  return (
    <Form>
      Creating school  form
    </Form>
  )
}

export default CreatingSchoolForm
